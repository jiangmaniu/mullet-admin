upstream www-mullet {
    server 172.31.26.28:3000;
}

server {
    listen  80;
    server_name mullet.top www.mullet.top;

    return 301 https://$host$request_uri;
 }

server {
    listen 443 ssl http2;
    server_name mullet.top www.mullet.top;

    if ($host != 'www.mullet.top') {
        rewrite ^/(.*) https://www.mullet.top/$1 permanent;
    }

    access_log logs/www.mullet.top.access-ssl.log;
    error_log logs/www.mullet.top.error-ssl.log;

    include ssl.conf;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header X-Real_IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://www-mullet;
     }
}
